const createRecipe = (newRecipe) => {
  alert(name);
};

if (document.querySelector('.form')) {
  document.querySelector('.form').addEventListener('submit', (event) => {
    event.preventDefault();
    // const newRecipe = {};
    // newRecipe.name = document.getElementById('name').value;
    // newRecipe.description = document.getElementById('description').value;
    // newRecipe.steps = document.getElementById('steps').value;
    // newRecipe.link = document.getElementById('link').value;

    // createRecipe(name);
  });
}
