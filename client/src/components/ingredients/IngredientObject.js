export class IngredientObject{
    constructor(idIngredient, name, amount, measure){
        this.idIngredient = idIngredient;
        this.name = name;
        this.amount = amount;
        this.measure = measure;
    }
}